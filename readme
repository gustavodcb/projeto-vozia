crie um arquivo .env e adicione o token "DISCORD_TOKEN=SEU_TOKEN_DO_BOT_AQUI", sempre que der commit apague o arquivo se nao da erro!

Para executar o bot execute no terminal "node index.js" dessa forma ele ficara online.

Para encerrar dar ctrl + C no terminal