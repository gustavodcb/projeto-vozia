// Arquivo: sharedState.js

// Este arquivo tem uma única responsabilidade:
// criar e exportar o estado que será compartilhado por todo o bot.
const activeRecordings = new Map();

module.exports = {
    activeRecordings
};